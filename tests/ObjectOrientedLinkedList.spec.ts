import {ObjectOrientedLinkedList} from "../src/ObjectOrientedLinkedList";

describe('ObjectOrientedLinkedList', () => {
  let list: ObjectOrientedLinkedList<number>;

  beforeEach(() => {
    list = new ObjectOrientedLinkedList<number>();
  });

  describe('push', () => {});
  describe('delete', () => {});
  describe('at', () => {});
  describe('find', () => {});
  describe('some', () => {});
  describe('forEach', () => {});
  describe('map', () => {});
  describe('filter', () => {});
  describe('isEmpty()', () => {});
  describe('size()', () => {});
});
