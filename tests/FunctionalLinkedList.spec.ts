import {FunctionalLinkedList} from "../src/FunctionalLinkedList";

describe('ObjectOrientedLinkedList', () => {
  let list: FunctionalLinkedList<number>;

  beforeEach(() => {
    list = new FunctionalLinkedList<number>();
  });

  describe('push', () => {});
  describe('delete', () => {});
  describe('at', () => {});
  describe('find', () => {});
  describe('some', () => {});
  describe('forEach', () => {});
  describe('map', () => {});
  describe('filter', () => {});
  describe('isEmpty()', () => {});
  describe('size()', () => {});
});
